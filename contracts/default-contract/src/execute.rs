// HERE WE HAVE THE EXECUTE FUNCTION

use crate::state::{STATE};
use crate::error::ContractError;
use cosmwasm_std::{DepsMut, MessageInfo, Response,};

pub fn execute1 () -> Result<Response, ContractError> {
    unimplemented!()
}
